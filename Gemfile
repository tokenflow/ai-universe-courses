# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }
# Use a stable Ruby supported widely in CI and GitHub Pages build images.
# If you want to experiment locally with newer Rubies, avoid pinning to an
# unreleased/bleeding-edge minor version here; keep constraint flexible.
ruby "~> 3.3", ">= 3.3.0"
gem "jekyll"
gem 'jekyll-feed'
gem 'jekyll-readme-index'
gem 'jemoji'
gem 'webrick'

# gem "rails"
